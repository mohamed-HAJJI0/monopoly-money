services:
  monopoly:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: monopoly-money
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
      - PORT=8080
    restart: unless-stopped