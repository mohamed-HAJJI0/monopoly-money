# The origins allowed to make calls to the server (where the frontend is hosted). Can be multiple domains separated by commas.
SERVER_ALLOWED_ORIGINS=http://localhost:3000

# When set to "true", newly created games will be populated with fake players and transactions for testing.
# Omit or set to "false" to create clean games without default players.
# INJECT_FAKE_DATA=false
